After setup XAMPP complete:

Follow below the step:
1. Open C:\xampp\xampp-control.exe, make sure click the start button to open Apache and MySQL
1. In the browser, type localhost/misaimanmalaysia go in the page (This url for all person can see the page.)
2. In the browser, type localhost/misaimanmalaysia/admin.php go in the admin page (This url for the administratro to see the page.)